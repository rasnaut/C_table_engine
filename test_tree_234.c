int main() {
    // Node234* root = create_node(1);
    // insert(&root, 10, NULL);
    // insert(&root, 20, NULL);
    // insert(&root, 5, NULL);
    // insert(&root, 6, NULL);
    // insert(&root, 12, NULL);
    // insert(&root, 30, NULL);
    // insert(&root, 7, NULL);
    // insert(&root, 17, NULL);

    // print_tree(root, 0);
    return 0;
}